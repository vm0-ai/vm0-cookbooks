version: "1.0"

agents:
  fetch-stores:
    description: "Store data assistant that helps users fetch and analyze store information"
    framework: claude-code
    instructions: AGENTS.md
    environment:
      DUMPLING_AI_API_KEY: ${{ secrets.DUMPLING_AI_API_KEY }}
